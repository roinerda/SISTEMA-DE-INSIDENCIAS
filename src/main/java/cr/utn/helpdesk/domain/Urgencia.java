package cr.utn.helpdesk.domain;

/**
 * Urgencia declarada al registrar la incidencia (HU-01).
 * Es la segunda entrada del calculo de prioridad (HU-02).
 */
public enum Urgencia {
    BAJA,
    MEDIA,
    ALTA
}
