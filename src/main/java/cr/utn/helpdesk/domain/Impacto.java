package cr.utn.helpdesk.domain;

/**
 * Impacto declarado al registrar la incidencia (HU-01).
 * Es una de las dos entradas del calculo de prioridad (HU-02).
 */
public enum Impacto {
    BAJO,
    MEDIO,
    ALTO
}
